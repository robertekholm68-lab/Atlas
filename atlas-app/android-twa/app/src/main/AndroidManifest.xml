<?xml version="1.0" encoding="utf-8"?>
<!-- Askr som Trusted Web Activity.

     Ingen WebView, ingen egen aktivitet: LauncherActivity ur
     android-browser-helper öppnar DEFAULT_URL i Chrome som en app — utan
     adressfält när assetlinks.json på sajten pekar på den här nyckeln, med
     Chromes verktygsfält om verifieringen saknas. Appen fungerar i båda fallen.

     Allt Chrome har finns i appen: Web Bluetooth (pulsband), mikrofon, notiser.
     Mikrofonbryggan i WebView-skalet behövs inte längre. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Translucent.NoTitleBar">

        <!-- Talar om för Chrome vilken sajt appen gör anspråk på. Sajten svarar
             med .well-known/assetlinks.json som pekar tillbaka på nyckeln. -->
        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />

        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:exported="true"
            android:label="@string/app_name">

            <!-- ADRESSEN FÅR INTE ÄNDRAS: samma som WebView-skalet hade hårdkodad. -->
            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="https://robertekholm68-lab.github.io/Atlas/atlas2.html" />
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:resource="@color/askr_bg" />
            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:resource="@color/askr_bg" />
            <meta-data
                android:name="android.support.customtabs.trusted.SCREEN_ORIENTATION"
                android:value="portrait" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Länkar till sajten öppnas i appen, inte i webbläsaren. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="robertekholm68-lab.github.io"
                    android:pathPrefix="/Atlas/" />
            </intent-filter>
        </activity>
    </application>
</manifest>
